@{
    string layout = ViewBag.Layout;
}
<div class="container">
    <div class="article-detail">
        <div class="dok-heading">
            <h1 class="mb-0">@(Html.CurrentPage().Name)</h1>
        </div>
        <hr>
        <div class="article-content">
            @Html.Raw(Html.CurrentPage().Content)
        </div>
    </div>
</div>
